import './globals.css';
import type { Metadata } from 'next';
import { Inter, JetBrains_Mono } from 'next/font/google';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-jetbrains-mono',
  display: 'swap',
});

export const metadata: Metadata = {
  metadataBase: new URL('https://mediscribe-ai-alpha.vercel.app'),
  title: 'MediScribe AI — AI Clinical Documentation',
  description:
    'AI-powered clinical documentation that gives doctors their time back. Three AI agents listen to consultations and generate complete SOAP notes in 30 seconds.',
  openGraph: {
    title: 'MediScribe AI',
    description: 'AI-powered clinical documentation for modern healthcare.',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased`}
        suppressHydrationWarning
      >
        {children}
      </body>
    </html>
  );
}
